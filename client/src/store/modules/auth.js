const state = {
  isAuthenticated: false
};

const getters = {
  isAuthenticated: (state) => state.isAuthenticated
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
